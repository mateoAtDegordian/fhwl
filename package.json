{
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html README.md favicon.ico sunce.svg *.webp dist/ && cp -R gallery dist/gallery"
  },
  "devDependencies": {
    "wrangler": "^4.20.5"
  }
}
